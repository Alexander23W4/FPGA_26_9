/******************************************************************************
* common/crc32.h —— CRC32（和 PC 端脚本用的完全同一个算法）
******************************************************************************/

#ifndef COMMON_CRC32_H
#define COMMON_CRC32_H

#include "xil_types.h"

/* 标准 CRC32：多项式 0xEDB88320（反射），初值/终值都取反。
 * 校验值必须和 scripts/pc 下的 .ps1 里的 Get-Crc32 一致，
 * 否则 PC 算出来和板子对不上。 */
u32 crc32_calc(const u8 *p, u32 n);

#endif /* COMMON_CRC32_H */
