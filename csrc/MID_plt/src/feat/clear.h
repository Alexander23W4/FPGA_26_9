/******************************************************************************
* feat/clear.h —— feature: 清空 eMMC
*
*   把图像目录里登记过的每一张图所占据的块全部抹成 0x00，然后清空目录。
*   这样既不依赖 eMMC 的擦除命令（各家实现有差异），又能立刻回读验证。
******************************************************************************/

#ifndef FEAT_CLEAR_H
#define FEAT_CLEAR_H

/* 清空 eMMC 上已登记的图像 + 清空图像目录。返回 0 = 成功 */
int feat_emmc_clear_run(void);

#endif /* FEAT_CLEAR_H */
